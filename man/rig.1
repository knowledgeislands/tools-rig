.TH RIG 1 "2026-09-16" "Knowledge Islands" "User Commands"
.SH NAME
\&
.PP
rig \- describe and manage a personal working setup
.SH SYNOPSIS
\&
.B rig show
.RI [ \-\-profile " NAME" ]
.br
.B rig list
.RI [ \-\-category " ID" ]
.RI [ \-\-profile " NAME" ]
.br
.B rig explain
.I TOOL
.br
.B rig diag
.br
.BI "rig completion " bash|zsh
.br
.B rig
.RB [ \-h | \-\-help | \-V | \-\-version ]
.SH DESCRIPTION
\&
.PP
Rig is the declarative description and manager of a person's working setup.
Its catalogue records tools, categories, purposes, rationale, relationships,
supported platforms, profiles, and provider bindings.
.PP
Catalogue queries parse inert configuration and do not invoke provider code.
Package and configuration systems retain authority over their native manifests,
resolution, configuration, and state.
.SH INSTALLATION
\&
.PP
Install the latest available release, falling back to the current
.B main
build while Rig is pre-release:
.PP
.nf
curl -fsSL https://raw.githubusercontent.com/knowledgeislands/tools-rig/main/install.sh | bash
.fi
.PP
The installer writes the executable to
.I ~/.local/bin
and the manual beneath
.I ${XDG_DATA_HOME:-$HOME/.local/share}/man/man1
by default. Set
.B RIG_INSTALL_DIR
or
.B RIG_MAN_INSTALL_DIR
to choose different locations. Set
.B RIG_VERSION
to install a specific release tag or branch.
.SH COMMANDS
\&
.TP
.B rig show
Describe the resolved default profile.
.TP
.BI "rig show \-\-profile " NAME
Describe the named resolved profile without changing the configured default.
.TP
.B rig list
List every declared catalogue tool in deterministic order.
.TP
.BI "rig list \-\-category " ID
Narrow the list to tools in the declared category.
.TP
.BI "rig list \-\-profile " NAME
Narrow the list to tools selected by the resolved profile.
The category and profile filters may be combined.
.TP
.BI "rig explain " TOOL
Show the tool's identity, category, purpose, rationale, supported platforms,
relationships, profile membership, and compatible provider binding.
.TP
.B rig diag
Print a non-mutating diagnostic snapshot containing Rig and Bash versions,
the invoked executable, active platform, effective XDG directories, root
configuration and fragment count, configuration validity, schema, and default
profile when available. No provider is invoked.
.TP
.BI "rig completion " bash|zsh
Print completion source for the selected shell.
.TP
.B rig help
Show top-level command help.
.SH SHELL COMPLETION
\&
.PP
The
.B rig completion bash
and
.B rig completion zsh
print completion definitions for the shipped command and option surface.
.PP
For Bash, source the definition from the shell configuration:
.PP
.nf
source <(rig completion bash)
.fi
.PP
For Zsh, write the definition to a file named
.I _rig
in a directory on
.B fpath
before
.B compinit
runs. For example:
.PP
.nf
mkdir -p ~/.zsh/completions
rig completion zsh > ~/.zsh/completions/_rig
fpath=(~/.zsh/completions $fpath)
autoload -Uz compinit && compinit
.fi
.SH OPTIONS
\&
.TP
.BR \-h , " \-\-help"
Show command help.
.TP
.BR \-V , " \-\-version"
Print the Rig version.
.SH CONFIGURATION
\&
.PP
Rig reads
.I ${RIG_CONFIG_HOME}/rig.conf
and then optional
.I ${RIG_CONFIG_HOME}/conf.d/*.conf
fragments in bytewise filename order.
When
.B RIG_CONFIG_HOME
is unset, its default is
.IR ${XDG_CONFIG_HOME:-$HOME/.config}/rig .
.PP
Configuration uses Rig's versioned, INI-shaped grammar. Values are literal;
Rig does not source configuration, evaluate commands, interpret shell quoting,
or perform general environment expansion.
.SH ENVIRONMENT
\&
.TP
.B RIG_CONFIG_HOME
Override the Rig configuration directory.
.TP
.B RIG_DATA_HOME
Override the Rig data directory.
.TP
.B RIG_STATE_HOME
Override the Rig state directory.
.TP
.B RIG_CACHE_HOME
Override the Rig cache directory.
.TP
.B RIG_PLATFORM
Override the active catalogue platform used by profile and binding resolution.
By default Rig maps Bash
.B OSTYPE
values beginning with
.B darwin
to
.B macos
and values beginning with
.B linux
to
.BR linux .
.TP
.B XDG_CONFIG_HOME
Set the base configuration directory when
.B RIG_CONFIG_HOME
is unset. The fallback is
.IR $HOME/.config .
.TP
.B XDG_DATA_HOME
Set the base data directory when
.B RIG_DATA_HOME
is unset. The fallback is
.IR $HOME/.local/share .
.TP
.B XDG_STATE_HOME
Set the base state directory when
.B RIG_STATE_HOME
is unset. The fallback is
.IR $HOME/.local/state .
.TP
.B XDG_CACHE_HOME
Set the base cache directory when
.B RIG_CACHE_HOME
is unset. The fallback is
.IR $HOME/.cache .
.SH FILES
\&
.TP
.I ${XDG_CONFIG_HOME:-$HOME/.config}/rig/rig.conf
Default root configuration file. Schema 1 uses
.B [rig]
with
.B schema
and
.BR default-profile ;
.B [category.ID]
with
.B name
and
.BR purpose ;
.B [tool.ID]
with
.BR name ,
.BR category ,
.BR purpose ,
.BR rationale ,
and one or more
.B platform
fields; and
.B [profile.ID]
with repeated
.B profile
or
.B tool
fields. Provider and binding sections retain their native locator and argument
data without executing it while configuration is loaded.
.B [provider.ID]
requires
.B adapter
and accepts
.BR command ,
.BR executable ,
.BR manifest ,
and repeated
.B argument
or
.B capability
fields.
.B [binding.TOOL.PROVIDER]
requires
.B kind
and
.B locator
and accepts repeated
.B platform
or
.B argument
fields.
.B [publication.ID]
requires
.BR profile ,
.BR title ,
.BR base-url ,
and
.BR publisher .
.PP
A minimal catalogue is:
.PP
.nf
[rig]
schema = 1
default-profile = default

[category.navigation]
name = Navigation
purpose = Move through Knowledge Islands

[tool.mgit]
name = MGit
category = navigation
purpose = Navigate related repositories
rationale = Keeps repository context visible
platform = macos

[profile.default]
tool = mgit
.fi
.TP
.I ${XDG_CONFIG_HOME:-$HOME/.config}/rig/conf.d/*.conf
Optional configuration fragments.
.TP
.I ${XDG_DATA_HOME:-$HOME/.local/share}/rig
Default data directory.
.TP
.I ${XDG_STATE_HOME:-$HOME/.local/state}/rig
Default state directory.
.TP
.I ${XDG_CACHE_HOME:-$HOME/.cache}/rig
Default cache directory.
.SH DEVELOPMENT INSTALLATIONS
\&
.PP
For development against a local checkout, run
.B ./install.sh --link
to link the current executable and manual into
.B RIG_INSTALL_DIR
(default
.IR ~/.local/bin )
and
.B RIG_MAN_INSTALL_DIR
(default: the XDG data directory beneath
.IR man/man1 ).
The linked command follows changes to the checkout and does not download a
release. Re-run link mode after moving the checkout. Use
.B ./bin/rig
directly or
.B man -l man/rig.1
to inspect the checked-out artifacts without changing installed paths.
.SH EXIT STATUS
\&
.TP
.B 0
Success.
.TP
.B 1
Diagnostic finding or operational failure. For
.B rig diag
this means configuration is missing or invalid.
.TP
.B 2
Invalid command syntax, unknown query identity, or invalid configuration.
.SH SEE ALSO
\&
.BR bash (1),
.BR brew (1),
.BR chezmoi (1),
.BR uv (1)
